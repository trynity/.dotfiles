#!/usr/bin/env ruby

require 'opencnam'
require 'yaml'

CONFIG = YAML::load(File.open("#{Dir.home}/.callerid"))

client = Opencnam::Client.new(
  :account_sid => CONFIG['account_sid'],
  :auth_token => CONFIG['auth_token'],
)

puts client.phone ARGV[0]
