try:
    import readline
except ImportError:
    print "Module readline not available."
else:
    import rlcompleter
    readline.parse_and_bind("tab: complete")

class Quit:
  def __repr__(self):
    import sys
    sys.exit()
exit = quit = Quit()
del Quit
